name: multiple_stream_builder
description: Flutter widgets to replace multiple nested StreamBuilder widgets
version: 3.0.1
homepage: https://github.com/glitchedmob/flutter_multiple_stream_builder

environment:
  sdk: ">=2.17.3 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
